This is intended to test the maximum heap and stack sizes of a certain compiler and computer environment.
The program is designed to recursively allocate memory on the stack until an overflow is produced.
It will crash, it is supposed to do so.